# 修仙小游戏 · 项目计划书

| 项目 | 内容 |
| --- | --- |
| 项目名称 | 修仙录（暂定） |
| 文档版本 | v1.0 |
| 编写日期 | 2026-09-11 |
| 文档状态 | 计划草案 |
| 技术形态 | 网页端 H5 游戏（Web + 后端服务） |

---

## 1. 项目概述

### 1.1 项目背景
修仙题材在中文用户群体中拥有广泛的受众基础，但市面上的产品多为重度 RPG 或放置类手游，缺少一款**轻量、易上手、可在浏览器直接体验**的修仙小游戏。本项目目标是用较小的开发成本，做出一款具备"注册成长 → 灵根设定 → 地图探索 → 回合制战斗 → 炼丹养成"核心闭环的网页小游戏，验证玩法可行性后再考虑扩展。

### 1.2 项目目标
- 实现用户注册 / 登录体系，建立玩家身份。
- 通过"灵根测试"给每位玩家一个**唯一灵根属性**，影响后续成长路线。
- 提供格子地图探索玩法：随机分布资源与野怪，移动即触发事件。
- 提供回合制战斗，结合功法、武器、状态进行策略对抗。
- 提供背包与炼丹系统，形成"探索 → 战斗 → 产出 → 强化"的成长闭环。

### 1.3 游戏定位
- **类型**：放置探索 + 回合制战斗的轻量修仙挂机小游戏。
- **平台**：桌面 / 移动端浏览器（响应式 H5）。
- **单次时长**：3–10 分钟。
- **核心乐趣**：灵根随机性、地图探索未知感、战斗策略、炼丹养成。

### 1.4 目标用户
- 喜欢修仙 / 仙侠题材的泛二次元与文字游戏玩家。
- 碎片时间多、希望"随时开局随时收手"的休闲玩家。

---

## 2. 游戏世界观与核心设定

### 2.1 核心概念
- **灵根**：决定玩家修炼属性的天赋。灵根属性影响功法适配、技能伤害加成。
- **境界**：玩家等级 / 修为的统称（练气 → 筑基 → 金丹 → 元婴……），作为成长里程碑。
- **地图（秘境）**：玩家探索的格子世界，不同地图对应不同等级区间与产出。

### 2.2 灵根属性表

| 灵根 | 类别 | 说明 | 战斗偏向 |
| --- | --- | --- | --- |
| 金 | 普通 | 锋锐、防御 | 高物防、穿透 |
| 木 | 普通 | 生机、回复 | 治疗、持续伤害 |
| 水 | 普通 | 柔润、控制 | 减速、封印 |
| 火 | 普通 | 暴烈、爆发 | 高爆发、灼烧 |
| 土 | 普通 | 厚重、护体 | 高血、减伤 |
| 雷 | 变异 | 迅疾、麻痹 | 高速度、麻痹 |
| 冰 | 变异 | 严寒、冻结 | 控制、冻结 |
| 空（空灵根） | 变异 / 特殊 | 无属性、兼容 | 全能适配、稀有 |

> 说明：普通灵根单属性；变异灵根（雷、冰）为高级特性；**空灵根**为极稀有，可兼容所有功法且不互斥，作为"欧皇"彩蛋属性。

- **神识（spiritual sense）**：独立于灵根的**成长型属性**，随境界 / 修为提升而增强，决定地图**迷雾可见范围**（半径），是**探图的核心能力**。无论单人还是多人，地图均存在战争迷雾：玩家只能看到以自身坐标为圆心、半径 `R = 基础值 + f(神识)` 范围内的格子；神识越大，探图越远。

---

## 3. 核心系统详细设计

### 3.1 用户系统

**功能**
- 注册：用户名 + 密码（哈希存储），或邮箱 / 第三方（二期）。
- 登录：账号密码校验，签发会话令牌（JWT）。
- 个人主页：境界、灵根、战力、背包容量、已学功法。

**关键字段**
```
User: id, username(登录账号), nickname(昵称/展示名), userNumber(仙途编号,6位全局唯一首位非0),
      passwordHash(BCrypt,内含盐), spiritRoots(JSON:["GOLD","WOOD"],测试后赋值), realmCode(境界 code,LIANQI/JIANZHU/...), realmLevel(1-9 层,1-3 初期/4-6 中期/7-9 后期),
      hp, maxHp, attack, defense, speed, spiritualSense(神识),
      mana(灵力), maxMana(灵力上限), exp, createdAt

- username：登录账号（道号），不可重复；nickname：展示用昵称，可后续修改。
- userNumber：注册时系统自动分配的 6 位数字编号（100000-999999），全局唯一、首位非 0，作为玩家对外标识（仙途编号）。
- mana / maxMana：灵力与上限，施放功法时消耗（M4 战斗实装），初始各 50。
- 神识（spiritualSense）初始值由灵根稀有度微调，之后随境界成长；迷雾可见半径 `R = 基础值 + f(神识)`，单人 / 多人地图均适用，是探图的主要能力。
```

### 3.2 灵根测试系统

**灵根（code 集合，无其他属性）**
- 灵根**不再作为可配置表**存在，不存任何战斗加成 / 稀有度 / 颜色 / 描述等扩展字段。
- **硬编码映射**：后端 `common/SpiritRoots` 提供静态 `CODES` 与 `NAMES`（GOLD→金、WOOD→木、WATER→水、FIRE→火、EARTH→土、THUNDER→雷、ICE→冰、VOID→空），8 种灵根固定不变。
- 灵根**唯一的作用**是决定玩家可修炼的功法（功法系统按 code 限定可学门类，见后续功法系统）。
- 展示层 `SpiritRootInfo` 只含 `{code, name}` 两个字段。

**用户灵根（JSON 内嵌，支持单/双/多灵根）**
- 一个玩家可能持有一个或多个灵根（单 / 双 / 多灵根），**没有所谓的主灵根 / 副灵根**，灵根之间完全平等。
- 字段：`t_user.spirit_root_code VARCHAR(1000)`，内容为 JSON 字符串数组，元素为灵根 code。
- 示例：`["GOLD"]` 单灵根；`["GOLD","WOOD"]` 双灵根；`["THUNDER","ICE"]` 双变异灵根。
- Java 侧：`User.spiritRoots: List<String>`，由 `StringListJsonTypeHandler`（Jackson）与 VARCHAR 字段双向序列化。
- 灵根数量由 JSON 数组长度决定：`spiritRoots.length` 即灵根数量，前端按需展示。

**流程**
1. 新用户注册完成后，强制进入"灵根测试"。
2. 系统随机 / 固定出 **5 道题目**，每题 4 个选项。
3. 每个选项预置对各属性的权重分。
4. 统计 5 题得分 → 计算各属性总分 → 取最高者为**首个灵根**。
5. 根据规则判定是否为变异灵根 / 空灵根，写入 `User.spiritRoots`（JSON 数组）。

**判定规则（算法草案）**
- 基础属性（金木水火土）取总分最高者作为首个灵根。
- 若某变异属性（雷/冰）总分达到阈值（如 ≥ 8 且 ≥ 普通最高分），则升级为变异灵根。
- 若 5 题全部选择"中性 / 均衡"选项（空灵倾向分最高），触发**空灵根**（极稀有）。
- **双灵根**：当第二高分（与首个灵根不同 code，且不是 VOID）达到次阈值（如 ≥ 首个灵根得分 × 0.5）时，一并存入 `User.spiritRoots` 数组；阈值可在 M2 实装时微调。
- 同分时按"火 > 水 > 金 > 木 > 土"优先级兜底；VOID 不作为双灵根的副灵根（空灵根独一）。
- 结果落库：直接将所有灵根 code 写入 `User.spiritRoots`（JSON 字符串数组，如 `["GOLD","THUNDER"]`），无主副标记。

**示例题库（见附录 A）**：每题 4 项，分别映射到灵根权重。

### 3.3 地图探索系统

**地图结构**
- 地图为 **N×N 方格**（如 8×8 或 10×10）。
- 每格状态之一：`空地` / `资源` / `野怪` / `出口` / `障碍物`。
- 进入地图时随机生成分布（保证可达、资源与野怪数量在合理区间）。

**移动规则**
- 玩家初始位于入口格，逐格**左右 / 上下移动**（按格子步进）。
- 移动到野怪格 → 触发战斗。
- 移动到资源格 → 自动收集（进入背包），格子变为空地。
- 移动到出口格 → 结算本次探索奖励并离开。
- 体力 / 步数限制：每次探索有步数上限，避免无限刷。

**迷雾（战争迷雾）**
- **单人地图同样存在战争迷雾**：未进入可见范围的格子对玩家不可见（不显示资源 / 野怪 / 地形细节）。
- 玩家维护一张**可见性网格**：以自身坐标为圆心、半径 `R = 基础值 + f(神识)` 的区域内实时可见；走过 / 探索过的格子留下"记忆"（暗色已知地形），离开范围后不再高亮但地形仍记忆。
- **神识是探图的主要能力**：神识越高，可见半径越大，越能提前规避野怪、发现稀有资源。
- 进入新格子时，以新坐标重新计算可见区域并"拨开"迷雾。

**地图难度分级**

| 地图 | 推荐境界 | 野怪等级 | 资源等级 |
| --- | --- | --- | --- |
| 后山竹林 | 练气 | 1–5 | 低级草药 |
| 幽冥洞窟 | 筑基 | 6–15 | 中品材料 |
| 上古秘境 | 金丹+ | 16+ | 稀有素材 |

### 3.4 战斗系统

**触发**：进入野怪格自动进入战斗界面。

**模式**：回合制（符合修仙策略感）。

**属性参与**
- 玩家：HP、攻击、防御、速度（受灵根、功法、武器、丹药加成）。
- 野怪：同套属性 + AI 行为（普攻 / 技能 / 逃跑倾向）。

**回合流程**
1. 比较双方速度决定先手。
2. 行动方选择：攻击 / 施放功法 / 使用道具 / 逃跑。
3. 结算伤害：`伤害 = max(1, 攻击 × 功法系数 − 防御 × 减伤)`，并附加灵根克制 / 灼烧 / 冰冻等状态。
4. 对方行动，循环直到一方 HP ≤ 0 或逃跑成功。

**结束条件**
- 一方 HP 归零 → 战斗结束（胜 / 负）。
- 逃跑：逃跑是否成功**取决于双方状态**：
  - 玩家速度 > 野怪速度 且 野怪未处于"狂暴 / 锁敌"状态 → 较高逃跑成功率。
  - 野怪处于"狂暴"或"精英 / Boss"状态 → 禁止 / 极低概率逃跑。
  - 玩家当前 HP 过低也可能影响（可设计为低血反而更易逃，或反之）。
  - 用概率公式：`逃跑成功率 = clamp( (玩家速度 − 野怪速度) × k + 基础值 , 0, 0.9 )`，并乘以状态修正系数。

**胜负奖励**
- 胜利：获得经验、灵石、可能掉落功法 / 武器 / 炼丹材料。
- 失败：损失部分灵石 / 扣境界进度（轻惩罚，避免劝退）。

### 3.5 背包系统

**结构**
```
Inventory: id, userId, capacity(容量上限)
InventoryItem: inventoryId, itemId, quantity
Item: id, name, type(材料/装备/丹药/功法), rarity, attrs(json)
```

**功能**
- 查看背包、按类型筛选（材料 / 装备 / 丹药 / 功法）。
- 使用丹药（回复 HP / 临时加成）。
- 装备武器 / 功法（影响战斗属性）。
- 容量上限，满后需"整理 / 扩容 / 丢弃"。

### 3.6 炼丹系统

**功能**
- 玩家收集炼丹材料（地图资源产出）。
- 选择丹方（配方）+ 消耗材料 → 炼丹（可加冷却 / 成功率）。
- 产出丹药：回血丹、聚气丹（加经验）、临时攻击丹等。

**丹方数据结构**
```
Recipe: id, name, requiredItems(json: {itemId:数量}),
        successRate, resultItemId, resultQuantity, cooldown
```

**炼丹流程**
1. 打开炼丹炉界面，选择丹方。
2. 校验背包材料是否充足。
3. 扣除材料，按 `successRate` 判定成功与否。
4. 成功 → 丹药入背包；失败 → 部分材料损耗（可设保底）。

### 3.7 功法与武器系统

| 类型 | 作用 | 与灵根关系 |
| --- | --- | --- |
| 功法 | 提供技能 / 属性加成（攻击、防御、速度、特殊状态） | 同属性灵根装备同属性功法 → 加成放大 |
| 武器 | 提供基础攻击 / 特殊效果 | 部分武器有灵根适配要求 |

- 玩家可同时装备 1 套功法 + 1 把武器（或可扩展多槽位）。
- 战斗时调用已装备功法释放技能。

---

## 4. 技术方案

### 4.1 技术栈（建议）
- **前端**：Vue 3 + Vite，Canvas 2D 渲染格子地图，Pinia 状态管理；**国风水墨视觉风格**（宣纸底 / 墨黑 / 朱砂印，借鉴 xiuxian 项目）。
- **后端**：Java 17 + **Spring Boot 3.x** 一套：
  - Web 层：Spring MVC（RESTful API）。
  - 安全：Spring Security + JWT（jjwt），密码哈希用 BCrypt。
  - 持久层：**MyBatis-Plus**（已采用，SQL 可控，与 xiuxian 项目一致）。
  - 缓存 / 会话：Spring Data Redis。
- **数据库**：MySQL 8（业务数据）+ Redis（会话 / 排行榜 / 缓存）。
- **构建与部署**：Maven 构建，Docker 容器化，Nginx 反代。
- **接口文档**：SpringDoc OpenAPI（Swagger UI）自动生成。
- **辅助**：Lombok（简化实体）、MapStruct（DTO 转换）、Hutool（工具）。

> 若追求更小成本，可一期用 **H2 内存库 + Spring Boot 单体**（无需额外装 MySQL），二期再切换 MySQL/Redis；MyBatis-Plus 与 JPA 二选一即可。

### 4.2 系统架构（简图）

```
┌────────────┐     HTTPS/JSON     ┌────────────────┐
│  前端 H5   │ ─────────────────▶ │  Spring Boot   │
│ Vue+Canvas │ ◀───────────────── │  后端 (Java)   │
└────────────┘                    └───────┬────────┘
                                         │
                            ┌────────────┼────────────┐
                            ▼            ▼            ▼
                       ┌────────┐  ┌────────┐  ┌────────┐
                       │ MySQL  │  │ Redis  │  │ 日志/  │
                       │        │  │ 缓存   │  │ 监控   │
                       └────────┘  └────────┘  └────────┘
```

**Spring Boot 后端分层**
```
Controller（REST 接口 / 参数校验）
   │
Service（业务逻辑：灵根判定、地图生成、战斗结算、炼丹）
   │
Manager（可选：跨服务编排，如战斗+掉落+经验一并处理）
   │
Mapper / Repository（MyBatis-Plus 或 JPA，负责 CRUD）
   │
MySQL + Redis
```

**实时通道（多人版需要）**
- 单人版：纯 REST（HTTP）即可，前端主动拉取地图状态。
- 多人版：额外引入 **WebSocket（Spring Boot + STOMP / Netty）**，用于玩家位置广播、PvP 挑战、地图事件实时推送。
- 地图实例状态（玩家坐标、迷雾、野怪）在 Redis 中按 `mapInstanceId` 缓存，降低 MySQL 压力。

### 4.3 数据库核心表（ER 概览）
- `user` 用户
- `spirit_root` 灵根定义（属性、类别、加成）
- `map_template` / `map_instance` 地图模板与实例
- `monster` 野怪
- `item` / `inventory` / `inventory_item` 物品与背包
- `technique` / `weapon` / `user_loadout` 功法、武器、装备槽
- `recipe` / `refine_log` 丹方与炼丹记录
- `combat_log` 战斗记录

### 4.4 关键接口（一期）
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | /api/auth/register | 注册 |
| POST | /api/auth/login | 登录 |
| POST | /api/spirit-root/test | 提交 5 题答案，返回灵根 |
| GET | /api/map/enter?mapId= | 生成并进入格子地图 |
| POST | /api/map/move | 移动一步，返回触发事件 |
| POST | /api/combat/action | 战斗中行动（攻击/技能/逃跑） |
| GET | /api/inventory | 查看背包 |
| POST | /api/alchemy/refine | 炼丹 |

---

## 5. 开发计划与里程碑

> **基线确认**：以下 M1–M6 为**单人版（一期）**，设计已确定，作为首个可上线交付物。
> 多人版（同图 / PvP / 迷雾）为**二期扩展**，见第 8 章，不阻塞一期进度。

| 阶段 | 周期(估) | 交付物 | 状态 |
| --- | --- | --- | --- |
| M1 基础框架 | 1.5 周 | 前后端脚手架、数据库、注册登录 | 一期 |
| M2 灵根系统 | 1 周 | 灵根题库 + 测试 + 属性绑定 | 一期 |
| M3 地图探索 | 2 周 | 格子地图生成、移动、资源/野怪事件、**战争迷雾 + 神识可见半径** | 一期 |
| M4 战斗系统 | 2 周 | 回合制战斗、功法/武器、逃跑逻辑 | 一期 |
| M5 背包+炼丹 | 1.5 周 | 背包管理、丹方、炼丹产出 | 一期 |
| M6 打磨上线 | 1 周 | 平衡数值、UI 美化、部署 | 一期 |

> 一期总周期约 8–9 周（单人 / 小团队）。二期（多人版）另见第 8 章里程碑。

---

## 6. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 数值不平衡（战斗过难/过易） | 体验差 | 早期建立数值表，M6 集中调参 |
| 灵根判定随机性过强 | 玩家不满 | 题库权重 + 兜底规则，保证分布合理 |
| 地图随机生成不可达 | 卡死 | 生成后做连通性校验 / 保底通路 |
| 炼丹材料通胀 | 经济崩 | 材料产出上限 + 丹方消耗递增 |
| 开发资源不足 | 延期 | 一期砍非核心（社交/排行榜后置） |

---

## 7. 附录

### 附录 A：灵根测试题库示例（5 题）

> 每题选项后括号为属性权重，仅示意。

**Q1 你独处时更偏爱？**
- A 锤炼兵器（金+2）
- B 侍弄花草（木+2）
- C 临水观鱼（水+2）
- D 围炉煮酒（火+2）

**Q2 面对强敌你倾向？**
- A 以守为攻（土+2）
- B 一击制敌（火+2）
- C 扰乱其势（雷+2）
- D 冻结时机（冰+2）

**Q3 修行中最向往？**
- A 万法归一（空+2）
- B 生生不息（木+2）
- C 坚不可摧（土+2）
- D 灼热焚敌（火+2）

**Q4 你的脾气更像？**
- A 冷静如渊（水+2）
- B 锐利如锋（金+2）
- C 暴烈如雷（雷+2）
- D 幽寒似冰（冰+2）

**Q5 若得一宝你选？**
- A 无锋重剑（金+1 土+1）
- B 碧玉灵珠（木+1 水+1）
- C 赤焰令（火+2）
- D 太虚心镜（空+2）

> 计分：累加各属性总分，最高者为灵根；雷/冰达阈值升级变异；空最高且触发隐藏条件为空灵根。

### 附录 B：伤害公式示例

```
基础伤害 = 攻击 × 功法系数
实际伤害 = max(1, 基础伤害 − 防御 × 0.6)
灵根克制加成 = 1.0 ~ 1.3（按属性相克表）
状态修正：灼烧 +持续伤害；冰冻 跳过敌方行动概率；麻痹 降速
逃跑成功率 = clamp((玩家速度 − 野怪速度) × 0.05 + 0.3, 0, 0.9) × 状态系数
```

### 附录 C：属性相克（草案）
金克木、木克土、土克水、水克火、火克金；雷/冰为变异，对普通有微弱压制，空灵根无克制关系但全适配。

---

## 8. 后续扩展路线（多人版 · 二期）

> 一期单人版已确定并先行交付（见第 5 章里程碑表）。本章为二期规划，**不阻塞一期**。二期在单人版数值与战斗稳定后启动。

### 8.1 设计基线
- 目标：**多名玩家进入同一张地图实例**，可互相看见（受迷雾 / 神识限制）、可发起玩家对战（PvP）；地图存在战争迷雾。
- 一期全部系统（灵根、地图、战斗、背包、炼丹）在二期中复用，PvP 直接复用 M4 回合制战斗。

### 8.2 多人同图
- 引入**地图实例（map instance）**：一张地图模板可生成多个实例，每个实例承载若干在线玩家（按等级 / 分服分片）。
- 玩家在实例内有独立坐标；移动、采集、战斗等事件通过 **WebSocket** 广播给同实例在线玩家。
- 实例状态（玩家坐标、野怪、资源）缓存于 **Redis**（按 `mapInstanceId`），定时 / 事件触发时落盘 MySQL（仅永久数据：战斗记录、掉落、经验）。

### 8.3 玩家间战斗（PvP）
- **触发**：两名玩家坐标相邻（或同格）时，一方可对另一方发起"切磋 / 约战"挑战；对方接受即进入回合制战斗。
- **分区**：设"和平地图"（需双方同意）与"竞技地图"（可强制开战），避免误伤劝退。
- **结算**：复用 M4 战斗公式；胜方获得荣誉 / 灵石，败方轻惩罚；神识高者在 PvP 中先手优势更明显。

### 8.4 迷雾系统与神识（基于一期 3.3 扩展）
- 一期**单人地图已实装战争迷雾**（见 3.3）：可见性网格三态（已探索记忆 / 当前可见 / 黑暗），半径由神识决定，神识是探图核心能力。
- 二期在单人迷雾之上**增加跨玩家显隐**：其他玩家仅在进入你的神识可见半径时才对你显示，形成"探敌"策略；你只能看到神识范围内的对手。
- 神识越高 → 可见半径越大 → 越早探测到野怪、资源、以及其他玩家。

### 8.5 技术补充（相对一期）
- 实时层：**WebSocket（Spring Boot + STOMP）**，含心跳保活、断线重连、位置插值。
- 状态层：地图实例状态存 Redis（Hash / ZSet），战斗与掉落等永久数据落 MySQL。
- 反作弊：战斗结算强制在服务端，前端仅负责渲染与输入，杜绝改血 / 改伤。

### 8.6 二期里程碑（估）

| 阶段 | 周期 | 交付物 |
| --- | --- | --- |
| M7 多人架构 | 1.5 周 | WebSocket、地图实例、Redis 状态同步 |
| M8 多人迷雾同步 | 1 周 | 跨玩家显隐、神识半径下的对手可见、实例内迷雾状态同步 |
| M9 PvP | 1.5 周 | 玩家挑战 / 接受、回合制复用、结算 |
| M10 联调打磨 | 1 周 | 同图体验、性能压测、反作弊 |

---

*—— 文档结束，后续可据评审结论细化各模块原型与数值表。*
